application led {
	
    sensor button: 10

    actuator led: 13

    -> off button {
        led is LOW
        goto off
	ifHighGoto on
    }

    on  button {
        led is HIGH 
        goto on 
	ifHighGoto off
    }
}
